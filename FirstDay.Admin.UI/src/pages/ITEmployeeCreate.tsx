import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ITEmployeeCreate() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Create IT Employee</CardTitle>
      </CardHeader>
      <CardContent>
        {/* IT Employee form will go here */}
      </CardContent>
    </Card>
  );
}