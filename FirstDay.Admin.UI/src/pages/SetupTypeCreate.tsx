import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SetupTypeCreate() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Create Setup Type</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Setup type form will go here */}
      </CardContent>
    </Card>
  );
}